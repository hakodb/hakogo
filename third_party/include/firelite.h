/* Auto-generated by cbindgen. Do not edit manually. */

#ifndef FIRELITE_H
#define FIRELITE_H

#pragma once

#include <stdarg.h>
#include <stdbool.h>
#include <stdint.h>
#include <stdlib.h>

#define INSERT 1

#define DELETE 2

typedef struct FL_Array FL_Array;

typedef struct FL_Batch FL_Batch;

typedef struct FL_CloudSync FL_CloudSync;

typedef struct FL_Config FL_Config;

typedef struct FL_Doc FL_Doc;

typedef struct FL_Engine FL_Engine;

typedef struct FL_NetSyncer FL_NetSyncer;

typedef struct FL_Query FL_Query;

/**
 * Raw (undecoded) document handle. `bytes` pins the storage-encoded
 * buffer (zero copies for inlined docs); decode on demand with
 * `fl_rawdoc_to_doc`. Bytes are opaque storage encoding — do not persist
 * or compare across versions.
 */
typedef struct FL_RawDoc FL_RawDoc;

/**
 * Slab of raw docs. Borrowed-handle contract mirrors FL_ResultSet:
 * `fl_rawresult_get` pointers die with `fl_rawresult_free`.
 */
typedef struct FL_RawResultSet FL_RawResultSet;

typedef struct FL_ResultSet FL_ResultSet;

typedef struct FL_Transaction FL_Transaction;

typedef struct FL_ViewDoc FL_ViewDoc;

typedef struct FL_Watch FL_Watch;

typedef void (*FL_OnSnapshotCallback)(const char *collection,
                                      const char *path,
                                      int32_t kind,
                                      void *user_data);

typedef bool (*FlWalkCallback)(const char *id,
                               uintptr_t id_len,
                               const uint8_t *bytes,
                               uintptr_t bytes_len,
                               void *userdata);

/**
 * View-walk callback: borrowed id + a borrowed view handle (valid for the
 * call only — do not free, do not retain). Return true to continue.
 */
typedef bool (*FlViewWalkCallback)(const char *id,
                                   uintptr_t id_len,
                                   const struct FL_ViewDoc *view,
                                   void *userdata);

#ifdef __cplusplus
extern "C" {
#endif // __cplusplus

struct FL_Engine *fl_engine_open(const char *path);

bool fl_engine_is_indexes_ready(struct FL_Engine *engine);

/**
 * Block until background work settles (index recovery + async index
 * updates + blob persistence + maintenance) or `timeout_ms` lapses.
 * Requires two consecutive clear samples, so use it before measuring.
 * Returns true when settled. See `FireLite::await_quiescent`.
 */
bool fl_engine_await_quiescent(struct FL_Engine *engine, uint64_t timeout_ms);

/**
 * Point-sample diagnostic as JSON (free with fl_string_free):
 * {"indexes_ready":b,"pending_index_ops":n,"index_backfills":n,
 *  "pending_blob_bytes":n,"queued_blob_items":n,
 *  "maintenance_running":b,"quiescent":b}.
 * Tells you WHAT is outstanding instead of guessing.
 */
char *fl_engine_quiescence_status(struct FL_Engine *engine);

struct FL_Config *fl_config_new(void);

void fl_config_free(struct FL_Config *config);

void fl_config_set_durability(struct FL_Config *config, int32_t mode);

void fl_config_set_encryption_key(struct FL_Config *config, const char *key);

/**
 * Set which collections should be encrypted.
 * collections_json: A JSON array of strings, e.g., '["secrets", "private_messages"]'
 */
int32_t fl_config_set_encrypted_collections(struct FL_Config *config, const char *collections_json);

void fl_config_set_audit_log(struct FL_Config *config, bool enabled, const char *path);

void fl_config_set_query_workers(struct FL_Config *config, uintptr_t count);

void fl_config_set_memory_limits(struct FL_Config *config,
                                 uintptr_t mmap_size,
                                 uintptr_t max_inlined_bytes);

void fl_config_set_storage_tuning(struct FL_Config *config,
                                  uintptr_t page_size,
                                  uintptr_t compaction_threshold,
                                  uintptr_t group_commit_max_ops);

void fl_config_set_blob_threshold(struct FL_Config *config, uintptr_t threshold_bytes);

/**
 * WAL headroom reservation in bytes (0 = off, default 4MB). Preallocated
 * ahead of the write position so steady-state appends never extend the
 * file. Sparse: consumes no disk until written. Ignored for Manual.
 */
void fl_config_set_wal_reserve_bytes(struct FL_Config *config, uint64_t bytes);

/**
 * Write-path phase breakdown (see engine::write_stats_report). Returns a
 * fresh C string the caller frees with fl_string_free. Counters reset.
 */
char *fl_debug_write_stats(void);

/**
 * Opens the engine using a custom config.
 * Note: This function takes ownership of the config and will free it automatically.
 */
struct FL_Engine *fl_engine_open_with_config(const char *path, struct FL_Config *config);

struct FL_Watch *fl_engine_watch(struct FL_Engine *engine,
                                 const char *collection,
                                 FL_OnSnapshotCallback callback,
                                 void *user_data_ptr);

void fl_watch_free(struct FL_Watch *watch);

void fl_engine_free(struct FL_Engine *engine);

struct FL_Doc *fl_doc_new(void);

void fl_doc_free(struct FL_Doc *doc);

int32_t fl_doc_insert_str(struct FL_Doc *doc, const char *key, const char *value);

int32_t fl_doc_insert_int(struct FL_Doc *doc, const char *key, int64_t value);

int32_t fl_doc_insert_float(struct FL_Doc *doc, const char *key, double value);

int32_t fl_doc_insert_bool(struct FL_Doc *doc, const char *key, bool value);

int32_t fl_doc_insert_null(struct FL_Doc *doc, const char *key);

int32_t fl_doc_insert_bin(struct FL_Doc *doc, const char *key, const uint8_t *data, uintptr_t len);

int32_t fl_engine_insert(struct FL_Engine *engine,
                         const char *collection,
                         const char *doc_id,
                         const struct FL_Doc *doc);

/**
 * Owned-doc insert: takes over the FL_Doc handle (no deep clone).
 * The handle is ALWAYS consumed — success or failure — do not use or free
 * `doc` after the call.
 */
int32_t fl_engine_insert_take(struct FL_Engine *engine,
                              const char *collection,
                              const char *doc_id,
                              struct FL_Doc *doc);

struct FL_Doc *fl_engine_get(struct FL_Engine *engine, const char *collection, const char *doc_id);

int32_t fl_engine_delete(struct FL_Engine *engine, const char *collection, const char *doc_id);

/**
 * Local-only delete: marks the key so no sync tailer or handshake
 * catch-up ever transmits it, then deletes normally (fresh tombstone
 * timestamp keeps the version clock advanced — handshake-stable).
 */
int32_t fl_engine_delete_local(struct FL_Engine *engine,
                               const char *collection,
                               const char *doc_id);

/**
 * Marks a collection local-only (`local != 0`) or rejoins it to sync.
 * A local-only collection never emits nor is caught up from the network.
 */
int32_t fl_engine_set_collection_local(struct FL_Engine *engine,
                                       const char *collection,
                                       int32_t local);

/**
 * Opts a key back into replication (future ops only).
 */
int32_t fl_engine_replicate_key(struct FL_Engine *engine,
                                const char *collection,
                                const char *doc_id);

/**
 * Opts a whole collection back into replication (clears flag + key marks).
 */
int32_t fl_engine_replicate_collection(struct FL_Engine *engine, const char *collection);

/**
 * Vacuum: purge a collection's tombstones. Emits no WAL op (never
 * replicates); drops the version so the next handshake pulls peer state.
 * Returns tombstones purged, or -1 on error.
 */
int32_t fl_engine_vacuum_collection(struct FL_Engine *engine, const char *collection);

struct FL_Batch *fl_batch_new(void);

void fl_batch_free(struct FL_Batch *batch);

int32_t fl_batch_set(struct FL_Batch *batch,
                     const char *collection,
                     const char *doc_id,
                     struct FL_Doc *doc);

int32_t fl_batch_delete(struct FL_Batch *batch, const char *collection, const char *doc_id);

int32_t fl_batch_commit(struct FL_Engine *engine, struct FL_Batch *batch);

struct FL_Query *fl_query_new(const char *collection);

void fl_query_free(struct FL_Query *query);

int32_t fl_query_where_eq_str(struct FL_Query *query, const char *field, const char *value);

int32_t fl_query_where_eq_bool(struct FL_Query *query, const char *field, bool value);

/**
 * Executes the query and deletes all matching documents.
 * Returns the number of deleted documents, or -1 on error.
 */
int32_t fl_query_delete(struct FL_Engine *engine, struct FL_Query *query);

/**
 * Local-only mass delete: marks every match so the wipe never leaves
 * this device, then deletes. See `fl_engine_delete_local`.
 */
int32_t fl_query_delete_local(struct FL_Engine *engine, struct FL_Query *query);

/**
 * Executes the query and applies the updates from 'patch_doc' to all matches.
 * Returns the number of updated documents, or -1 on error.
 */
int32_t fl_query_patch(struct FL_Engine *engine,
                       struct FL_Query *query,
                       const struct FL_Doc *patch_doc);

int32_t fl_query_where_eq_int(struct FL_Query *query, const char *field, int64_t value);

int32_t fl_query_where_ne_str(struct FL_Query *query, const char *field, const char *value);

int32_t fl_query_where_ne_int(struct FL_Query *query, const char *field, int64_t value);

int32_t fl_query_where_gt_str(struct FL_Query *query, const char *field, const char *value);

int32_t fl_query_where_gt_int(struct FL_Query *query, const char *field, int64_t value);

int32_t fl_query_where_gte_str(struct FL_Query *query, const char *field, const char *value);

int32_t fl_query_where_gte_int(struct FL_Query *query, const char *field, int64_t value);

int32_t fl_query_where_lt_str(struct FL_Query *query, const char *field, const char *value);

int32_t fl_query_where_lt_int(struct FL_Query *query, const char *field, int64_t value);

int32_t fl_query_where_lte_str(struct FL_Query *query, const char *field, const char *value);

int32_t fl_query_where_lte_int(struct FL_Query *query, const char *field, int64_t value);

int32_t fl_query_where_array_contains(struct FL_Query *query, const char *field, const char *value);

int32_t fl_query_where_array_contains_any(struct FL_Query *query,
                                          const char *field,
                                          struct FL_Array *array);

int32_t fl_query_where_not_in(struct FL_Query *query, const char *field, struct FL_Array *array);

int32_t fl_query_order_by(struct FL_Query *query, const char *field, bool ascending);

int32_t fl_query_limit(struct FL_Query *query, uintptr_t limit);

/**
 * Opt in to deferred blobs: matching docs come back with blob-backed
 * fields as `Value::BlobLink` placeholders (no blob-file reads).
 * Resolve later with `fl_doc_resolve_blobs`. Default off (eager).
 */
int32_t fl_query_defer_blobs(struct FL_Query *query, int defer);

/**
 * Resolve deferred blob fields of a query-returned doc in place.
 * No-op for docs without BlobLinks. Needs the owning collection (blob
 * addresses are per-shard).
 */
int32_t fl_doc_resolve_blobs(struct FL_Engine *engine, const char *collection, struct FL_Doc *doc);

int32_t fl_query_offset(struct FL_Query *query, uintptr_t offset);

int32_t fl_query_select_field(struct FL_Query *query, const char *field);

char *fl_query_execute(struct FL_Engine *engine, const struct FL_Query *query);

struct FL_ResultSet *fl_query_execute_to_handles(struct FL_Engine *engine,
                                                 const struct FL_Query *query);

uintptr_t fl_result_set_count(struct FL_ResultSet *results);

struct FL_Doc *fl_result_set_get_doc(struct FL_ResultSet *results, uintptr_t index);

void fl_result_set_free(struct FL_ResultSet *results);

struct FL_RawResultSet *fl_query_execute_raw(struct FL_Engine *engine,
                                             const struct FL_Query *query);

uintptr_t fl_rawresult_count(struct FL_RawResultSet *results);

struct FL_RawDoc *fl_rawresult_get(struct FL_RawResultSet *results, uintptr_t index);

void fl_rawresult_free(struct FL_RawResultSet *results);

/**
 * Borrowed byte view of a raw doc. Returns null on null handle; `*len_out`
 * (when non-null) receives the length. Valid until fl_rawresult_free —
 * zero copies for inlined docs.
 */
const uint8_t *fl_rawdoc_bytes(const struct FL_RawDoc *doc, uintptr_t *len_out);

/**
 * Borrowed id view of a raw doc (for keyset paging without decoding).
 * Rust Strings are NOT NUL-terminated, so the length goes through
 * `*len_out` (when non-null) — read `(ptr, len)`, do NOT treat as CStr.
 * Valid until fl_rawresult_free.
 */
const char *fl_rawdoc_id(const struct FL_RawDoc *doc, uintptr_t *len_out);

/**
 * Keyset anchor from a raw row. `id`-ordered queries bind the id with no
 * decode; other order fields decode the row ONCE per page (not per row).
 */
int32_t fl_query_start_after_raw(struct FL_Query *query, const struct FL_RawDoc *anchor_doc);

/**
 * The pointer resolver: decode a raw row into an owned FL_Doc (blob
 * fields inflated via the engine, same as a decoded query row).
 */
struct FL_Doc *fl_rawdoc_to_doc(struct FL_Engine *engine,
                                const struct FL_RawDoc *raw_doc,
                                const char *collection);

int64_t fl_cursor_walk(struct FL_Engine *engine,
                       const struct FL_Query *query,
                       FlWalkCallback callback,
                       void *userdata);

struct FL_ViewDoc *fl_view_get(struct FL_Engine *engine,
                               const char *collection,
                               const char *doc_id);

void fl_view_free(struct FL_ViewDoc *view);

uintptr_t fl_view_field_count(const struct FL_ViewDoc *view);

bool fl_view_has_field(const struct FL_ViewDoc *view, const char *key);

bool fl_view_get_int(const struct FL_ViewDoc *view, const char *key, int64_t *out);

bool fl_view_get_float(const struct FL_ViewDoc *view, const char *key, double *out);

int32_t fl_view_get_bool(const struct FL_ViewDoc *view, const char *key);

/**
 * Borrowed UTF-8 view of a String field. Returns null when missing or not
 * a String; `*len_out` (when non-null) receives the byte length. Valid
 * until fl_view_free — same borrowed contract as fl_rawdoc_bytes.
 */
const char *fl_view_get_str(const struct FL_ViewDoc *view, const char *key, uintptr_t *len_out);

/**
 * Borrowed view of a Binary field. Same lifetime contract as above.
 */
const uint8_t *fl_view_get_bytes(const struct FL_ViewDoc *view,
                                 const char *key,
                                 uintptr_t *len_out);

/**
 * Escape hatch: full owned decode of the pinned bytes (links unresolved —
 * follow with fl_doc_resolve_blobs when needed).
 */
struct FL_Doc *fl_view_to_doc(const struct FL_ViewDoc *view, const char *doc_id);

/**
 * One-call lazy scan: lends each row as a view (no decode, no owned
 * construction). Returns rows visited, -1 on error. Same no-reentry
 * contract as fl_cursor_walk.
 */
int64_t fl_cursor_walk_view(struct FL_Engine *engine,
                            const struct FL_Query *query,
                            FlViewWalkCallback callback,
                            void *userdata);

/**
 * Bulk result-set to JSON: one call, one JSON array string, no per-doc
 * DOM and no per-doc FFI round trips. Byte-identical to joining
 * `fl_doc_to_json` per row. Caller frees with `fl_string_free`.
 */
char *fl_result_set_to_json(struct FL_ResultSet *results);

char *fl_doc_to_json(const struct FL_Doc *doc);

const char *fl_last_error(void);

/**
 * Enable library diagnostic logging to stderr. Default OFF. Idempotent.
 */
void fl_log_enable_stderr(void);

/**
 * Disable library diagnostic logging to stderr. Default OFF. Idempotent.
 */
void fl_log_disable_stderr(void);

void fl_string_free(char *value);

int32_t fl_query_aggregate_count(struct FL_Query *query);

int32_t fl_query_aggregate_sum(struct FL_Query *query, const char *field);

int32_t fl_query_aggregate_avg(struct FL_Query *query, const char *field);

char *fl_query_execute_aggregation(struct FL_Engine *engine, const struct FL_Query *query);

int32_t fl_doc_insert_timestamp(struct FL_Doc *doc, const char *key, int64_t micros);

int32_t fl_doc_insert_server_timestamp(struct FL_Doc *doc, const char *key);

int32_t fl_engine_backup(struct FL_Engine *engine, const char *path);

int32_t fl_query_where_match(struct FL_Query *query, const char *field, const char *value);

int32_t fl_query_where_match_prefix(struct FL_Query *query, const char *field, const char *value);

int32_t fl_query_where_contains(struct FL_Query *query, const char *field, const char *value);

int32_t fl_query_where_starts_with(struct FL_Query *query, const char *field, const char *value);

char *fl_engine_list_collections(struct FL_Engine *engine);

struct FL_Array *fl_array_new(void);

void fl_array_free(struct FL_Array *array);

int32_t fl_array_append_str(struct FL_Array *array, const char *value);

int32_t fl_array_append_int(struct FL_Array *array, int64_t value);

int32_t fl_array_append_doc(struct FL_Array *array, const struct FL_Doc *doc);

/**
 * Takes the contents of 'child' and inserts it as a Map into 'parent'
 */
int32_t fl_doc_insert_doc(struct FL_Doc *parent, const char *key, const struct FL_Doc *child);

/**
 * Takes the contents of 'array' and inserts it into the document
 */
int32_t fl_doc_insert_array(struct FL_Doc *doc, const char *key, struct FL_Array *array);

int32_t fl_engine_patch(struct FL_Engine *engine,
                        const char *collection,
                        const char *doc_id,
                        const struct FL_Doc *updates);

/**
 * Creates a composite index from C++.
 * fields_json should be like: [{"field": "age", "desc": false}]
 */
uint32_t fl_engine_create_index(struct FL_Engine *engine,
                                const char *collection,
                                const char *fields_json);

/**
 * Simplified indexer: Create an index for a single field.
 */
int32_t fl_engine_create_simple_index(struct FL_Engine *engine,
                                      const char *collection,
                                      const char *field);

int32_t fl_engine_create_fts_index(struct FL_Engine *engine,
                                   const char *collection,
                                   const char *field);

char *fl_engine_list_indexes(struct FL_Engine *engine, const char *collection);

struct FL_Transaction *fl_transaction_begin(struct FL_Engine *engine);

struct FL_Doc *fl_transaction_get(struct FL_Engine *engine,
                                  struct FL_Transaction *tx,
                                  const char *collection,
                                  const char *doc_id);

int32_t fl_transaction_set(struct FL_Transaction *tx,
                           const char *collection,
                           const char *doc_id,
                           const struct FL_Doc *doc);

int32_t fl_transaction_commit(struct FL_Engine *engine, struct FL_Transaction *tx);

void fl_transaction_free(struct FL_Transaction *tx);

int32_t fl_engine_insert_subdoc(struct FL_Engine *engine,
                                const char *col,
                                const char *id,
                                const char *sub_col,
                                const char *sub_id,
                                const struct FL_Doc *doc);

int32_t fl_engine_compact(struct FL_Engine *engine);

char *fl_engine_get_stats(struct FL_Engine *engine);

int32_t fl_doc_insert_reference(struct FL_Doc *doc,
                                const char *key,
                                const char *target_collection,
                                const char *target_id);

/**
 * Given a document and a field name containing a Reference, fetch the target document.
 * Returns a new FL_Doc handle, or null if the field is not a reference or target not found.
 */
struct FL_Doc *fl_engine_get_by_ref(struct FL_Engine *engine,
                                    const struct FL_Doc *doc,
                                    const char *field_key);

int32_t fl_query_start_after(struct FL_Query *query, const struct FL_Doc *anchor_doc);

int32_t fl_query_start_at(struct FL_Query *query, const struct FL_Doc *anchor_doc);

int32_t fl_query_end_at(struct FL_Query *query, const struct FL_Doc *anchor_doc);

int32_t fl_query_end_before(struct FL_Query *query, const struct FL_Doc *anchor_doc);

char *fl_engine_get_audit_log(struct FL_Engine *engine);

int32_t fl_query_where_or_str(struct FL_Query *query, const char *field, const char *value);

int32_t fl_query_where_or_int(struct FL_Query *query, const char *field, int64_t value);

/**
 * Adds an IN filter: field IN [array_items]
 * This takes ownership of the FL_Array and frees it.
 */
int32_t fl_query_where_in(struct FL_Query *query, const char *field, struct FL_Array *array);

int32_t fl_engine_snapshot_indices(struct FL_Engine *engine);

void fl_config_set_compression(struct FL_Config *config, bool enabled, int32_t level);

/**
 * Hold background maintenance (checkpoint/compaction/purge/snapshots) for
 * deterministic benchmarks or hard latency bounds. Engine stays correct;
 * files grow until re-enabled. Default on.
 */
void fl_config_set_background_maintenance(struct FL_Config *config, bool enabled);

struct FL_NetSyncer *fl_net_syncer_new(struct FL_Engine *engine,
                                       const char *name,
                                       const char *room_key);

/**
 * Select discovery transports: 0 = mDNS (desktop default), 1 = UDP
 * broadcast (mobile default, no multicast), 2 = both (mixed groups — a
 * desktop joining mobile peers must opt into both or broadcast).
 * Takes effect at the next start().
 */
int32_t fl_net_syncer_set_discovery(struct FL_NetSyncer *syncer, int32_t mode);

int32_t fl_net_syncer_start(struct FL_NetSyncer *syncer, uint16_t port);

char *fl_net_syncer_status(struct FL_NetSyncer *syncer);

void fl_net_syncer_free(struct FL_NetSyncer *syncer);

struct FL_CloudSync *fl_cloud_sync_new(struct FL_Engine *engine,
                                       int32_t mode,
                                       const char *client_id,
                                       const char *room_name,
                                       const char *room_key,
                                       const char *auth_token);

/**
 * Creates a room-agnostic cloud SERVER. Not bound to any room: the server
 * accepts and persists any (room_name, room_key) pair its clients ask for and
 * routes sync to the matching room group.
 */
struct FL_CloudSync *fl_cloud_sync_server_new(struct FL_Engine *engine,
                                              const char *server_id,
                                              const char *auth_token);

/**
 * Creates an offline-first cloud CLIENT bound to a room of the caller's
 * choosing. The client picks the room (room_name + room_key) and later picks
 * the server via `fl_cloud_sync_start`.
 */
struct FL_CloudSync *fl_cloud_sync_client_new(struct FL_Engine *engine,
                                              const char *client_id,
                                              const char *room_name,
                                              const char *room_key,
                                              const char *auth_token);

int32_t fl_cloud_sync_start(struct FL_CloudSync *cloud_sync, const char *address);

char *fl_cloud_sync_status(struct FL_CloudSync *cloud_sync);

void fl_cloud_sync_stop(struct FL_CloudSync *cloud_sync);

void fl_cloud_sync_free(struct FL_CloudSync *cloud_sync);

#ifdef __cplusplus
}  // extern "C"
#endif  // __cplusplus

#endif  /* FIRELITE_H */
